# `@getonlane/mcp-auth`

See **[AGENTS.md](./AGENTS.md)** — that is the integration guide, and it is the
only copy.

Kept as a pointer rather than a duplicate on purpose: two files with the same
guidance drift, and the one that drifts is whichever the author of a change did
not have open. `AGENTS.md` is the emerging cross-tool convention
(<https://agents.md>); this file exists so Claude Code finds it too.
